version: '3.8'

services:
  retrace2-python:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: retrace2/python:latest
    volumes:
      - ..:/app
      - /tmp:/tmp
    working_dir: /app
    command: /bin/bash 